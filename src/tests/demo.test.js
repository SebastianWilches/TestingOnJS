
describe('Pruebas en <DemoComponent />', () => { 
    
    test ('Descripción de la prueba', () => {
        //1. Inicialización de variables
        const message1 = 'Hola mundo';
    
        //2. Estimulo
        const msessage2 = message1.trim();
    
        //3. Observamos si el comportamiento es el esperado
        expect(message1).toBe(msessage2);
    
    });

 })

describe('Pruebas en <DemoComponent />', () => { 
    //PRUEBAS
 })